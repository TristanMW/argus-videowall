# ── Secrets — NEVER commit these ─────────────────────────────────────────────
# Firebase Admin SDK / service-account keys (grant admin access to the project).
*firebase-adminsdk*.json
*serviceAccount*.json
*service-account*.json
*.serviceaccount.json
serviceAccountKey.json
.env
.env.*
secrets/
*.pem
*.key

# ── Runtime / build artifacts ────────────────────────────────────────────────
data/
node_modules/
*.log
.DS_Store

# ── Local go2rtc binary (downloaded, not committed) ──────────────────────────
go2rtc/go2rtc
go2rtc/go2rtc.exe
go2rtc/*.zip

# Runtime-written config backup created by update.sh
go2rtc/go2rtc.yaml.bak

# ── Firebase CLI local state ─────────────────────────────────────────────────
.firebase/
firebase-debug.log

# ── Firebase hosting (personal deployment — not part of the Docker/self-host repo)
firebase.json
.firebaserc
deploy-hosting.sh
setup.log
logs/

# ── Private to Wiltech — kept locally, never published ───────────────────────
# Firebase security rules for the argus-videowall project (deploy source of
# truth; not part of the self-host repo).
firestore.rules
database.rules.json
# Internal working notes.
docs/app_details.md
docs/monetisation.md
# Internal threat model: references the retired licensing design and open TODOs.
docs/security.md
